✅ APPROVED — `apps/api/src/routes/kb/index.ts` now includes a `mapDocument` mapper that converts the Prisma document relation shape into the UI-facing `collectionName` and `chunkCount` fields, and it is applied consistently to `GET /documents`, `GET /documents/:id`, and `PATCH /documents/:id`. I did not find any new issues introduced in this fix, so both branches are clear for Rex to run QA.
